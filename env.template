# ==========================================
# ContextLoom Configuration Template
# Copy this file to .env and adjust values.
# ==========================================

# --- NETWORK ---
# Ports to listen on. Change these if port 80/443 are busy (e.g. x-ui running).
# Default: 80 and 443
# HTTP_PORT=80
# HTTPS_PORT=443

# --- SECURITY ---
# A long, random string used to sign session tokens.
# Generate one via: openssl rand -hex 32
SECRET_KEY=change-this-to-a-secure-random-string

# --- DATABASE ---
# Connection string for the PostgreSQL database.
# Format: postgresql+asyncpg://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=postgresql+asyncpg://contextloom:securepassword@db:5432/contextloom

# --- LICENSE ---
# Path to the public key used to verify license signatures.
# In the standard appliance, this is pre-baked at /app/certs/public_key.pem.
# You likely do not need to change this unless using custom keys.
# LICENSE_PUBLIC_KEY_PATH=/app/certs/public_key.pem

# --- ENTERPRISE MODULES (Optional) ---

# [Module: Identity Shield (SSO)]
# OIDC_CLIENT_ID=your-client-id
# OIDC_CLIENT_SECRET=your-client-secret
# OIDC_DISCOVERY_URL=https://accounts.google.com/.well-known/openid-configuration
# OIDC_REDIRECT_URL=https://your-domain.com/auth/callback

# [Module: Compliance Stream (SIEM)]
# SIEM_HOST=splunk.internal
# SIEM_PORT=514
# SIEM_PROTOCOL=TCP

# [Module: Intranet Link (Custom CA)]
# Path to a CA Bundle file mounted into the container
# SSL_CA_BUNDLE_PATH=/app/certs/corporate-ca.crt
